{
    "channel": "{{ channel }}",
    "blocks": [{
        "type": "section",
        "fields": [
        {% for field in items['dataset1'].data[0] %}
          {
            "type": "mrkdwn",
	        "text": "{{ field }}"
          }{{ ", " if not loop.last else "" }}
        {% endfor %}
        ]
        
    }]
}